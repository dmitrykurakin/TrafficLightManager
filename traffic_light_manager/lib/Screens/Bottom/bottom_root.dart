import 'package:flutter/material.dart';

class BottomRoot extends StatelessWidget {
  const BottomRoot({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      color: Colors.red,
    );
  }
}
